import React from 'react';
import useTitle from '../../Components/Title/Title';

const About = () => {
    useTitle('About Us')
    return (
        <div>
            
        </div>
    );
};

export default About;