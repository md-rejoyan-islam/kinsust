import React from 'react';
import useTitle from '../../../Components/Title/Title';

const Photo = () => {
    useTitle("Photo")
    return (
        <div>
            
        </div>
    );
};

export default Photo;