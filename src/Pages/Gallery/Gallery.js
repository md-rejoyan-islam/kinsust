import React from 'react';
import useTitle from '../../Components/Title/Title';


const Gallery = () => {
    useTitle('Gallery')
    return (
        <div>
            
        </div>
    );
};

export default Gallery;