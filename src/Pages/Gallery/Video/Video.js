import React from 'react';
import useTitle from '../../../Components/Title/Title';

const Video = () => {
    useTitle('Video')
    return (
        <div>
            
        </div>
    );
};

export default Video;